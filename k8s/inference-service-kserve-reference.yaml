# 모니터링 — InferenceService 참고 (Git 빌드 이미지와 이름 일치)
# metadata.name = MCP 서버 등록 이름 csap-node-escape-probe
---
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: csap-node-escape-probe
  namespace: REPLACE_NAMESPACE
spec:
  predictor:
    containers:
      - name: kserve-container
        image: REPLACE_REGISTRY/csap-node-escape-probe:git-build
        ports:
          - name: user-port
            containerPort: 8080
        env:
          - name: PORT
            value: "8080"
          - name: MCP_SERVER_NAME
            value: "csap-node-escape-probe"
          - name: RUN_PROBE_ON_START
            value: "0"
        readinessProbe:
          tcpSocket:
            port: user-port
        livenessProbe:
          tcpSocket:
            port: user-port
